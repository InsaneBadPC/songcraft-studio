name: Retired public video renderer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        run: |
          echo "Public GitHub release rendering is retired; use the private Oracle worker."
          exit 1
