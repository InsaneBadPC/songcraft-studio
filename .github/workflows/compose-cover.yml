name: Retired public cover composer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed
        run: |
          echo "Public GitHub cover composition is retired; keep assets in private Supabase Storage."
          exit 1
