#!/usr/bin/env node

/**
 * Retired GitHub Actions cover composer.
 * 16:9 source images stay in private Supabase Storage; the active cover flow
 * must not create public release assets.
 */
console.error("This composer is retired. Keep cover sources private and use the Oracle/private cover pipeline.");
process.exit(1);
